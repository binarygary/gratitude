---
phase: 02-auth-session-hardening
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - app/Http/Middleware/HandleInertiaRequests.php
  - resources/js/Components/AppShell.tsx
  - README.md
requirements:
  - AUTH-01
  - AUTH-04
  - AUTH-05
  - AUTH-06
autonomous: true
must_haves:
  truths:
    - "Signed-out users can submit the embedded AppShell magic-link form with a Turnstile token or deterministic local bypass token."
    - "The sign-in menu exposes an explicit remember-device choice without creating a new auth page."
    - "Beta operators can find the exact Turnstile, cleanup command, session cookie, remember-device, and CSRF write-route posture in repository docs."
    - "Final Phase 2 quality gates pass after backend, frontend, and documentation wiring."
  artifacts:
    - path: "app/Http/Middleware/HandleInertiaRequests.php"
      provides: "Shared Turnstile and remember-device props"
      contains: "turnstile"
    - path: "resources/js/Components/AppShell.tsx"
      provides: "Embedded sign-in Turnstile and remember-device UI"
      contains: "Remember this device"
    - path: "README.md"
      provides: "Auth/session beta posture documentation"
      contains: "auth:prune-magic-links"
  key_links:
    - from: "app/Http/Middleware/HandleInertiaRequests.php"
      to: "resources/js/Components/AppShell.tsx"
      via: "Inertia shared props"
      pattern: "turnstile"
    - from: "resources/js/Components/AppShell.tsx"
      to: "app/Http/Controllers/Auth/MagicLinkController.php"
      via: "form fields `cf-turnstile-response` and `remember_device`"
      pattern: "cf-turnstile-response|remember_device"
    - from: "README.md"
      to: "routes/console.php"
      via: "cleanup command documentation"
      pattern: "auth:prune-magic-links"
---

<objective>
Finish the auth hardening surface: embedded Turnstile UI, visible remember-device choice, operator documentation, and final gate.

Purpose: Server protections need the existing AppShell form and repository docs to expose the right fields, copy, and operational commands.
Output: Inertia shared props, AppShell sign-in UI update, README auth/session posture, and full verification.
</objective>

<execution_context>
@/Users/garykovar/.codex/get-shit-done/workflows/execute-plan.md
@/Users/garykovar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-session-hardening/02-CONTEXT.md
@.planning/phases/02-auth-session-hardening/02-RESEARCH.md
@.planning/phases/02-auth-session-hardening/02-VALIDATION.md
@.planning/phases/02-auth-session-hardening/02-UI-SPEC.md
@AGENTS.md
@app/Http/Middleware/HandleInertiaRequests.php
@resources/js/Components/AppShell.tsx
@config/services.php
@config/auth.php
@config/session.php
@resources/js/bootstrap.js
@README.md
@routes/console.php
@.env.example
@.planning/phases/02-auth-session-hardening/02-04-SUMMARY.md

<interfaces>
Required shared prop shape:
```ts
turnstile: {
    enabled: boolean;
    site_key: string | null;
    bypass_token: string | null;
};
```

Required form fields:
```ts
useForm({
    email: '',
    'cf-turnstile-response': '',
    remember_device: false,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Share Turnstile and remember-device props to the frontend</name>
  <read_first>
    .planning/phases/02-auth-session-hardening/02-CONTEXT.md
    .planning/phases/02-auth-session-hardening/02-UI-SPEC.md
    app/Http/Middleware/HandleInertiaRequests.php
    config/services.php
    config/auth.php
    resources/js/Components/AppShell.tsx
  </read_first>
  <files>app/Http/Middleware/HandleInertiaRequests.php, resources/js/Components/AppShell.tsx</files>
  <action>Per D-02, D-03, and D-11, update `HandleInertiaRequests::share()` to include top-level `turnstile` props: `enabled` from `config('services.turnstile.enabled')`, `site_key` from `config('services.turnstile.site_key')`, and `bypass_token` from `config('services.turnstile.bypass_token')` only when the app environment is `local` or `testing` or Turnstile is disabled. Also add `auth.magic_link.remember_default` from `config('auth.magic_link.remember_default', false)` without changing the existing `auth.user` shape. Update `SharedProps` in `AppShell.tsx` for the new props and initialize the login form with `email`, `cf-turnstile-response`, and `remember_device`. When Turnstile is disabled, set `cf-turnstile-response` to the shared `bypass_token` or `local-turnstile-bypass`. Do not expose `TURNSTILE_SECRET_KEY` to Inertia props.</action>
  <acceptance_criteria>
    - `rg "turnstile|site_key|bypass_token|remember_default" app/Http/Middleware/HandleInertiaRequests.php resources/js/Components/AppShell.tsx` finds shared props and TypeScript usage.
    - `rg "TURNSTILE_SECRET_KEY|secret_key" app/Http/Middleware/HandleInertiaRequests.php resources/js/Components/AppShell.tsx` returns no secret exposure.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>AppShell receives only public Turnstile/remember configuration and can submit the server-required fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add embedded Turnstile widget and remember-device control</name>
  <read_first>
    .planning/phases/02-auth-session-hardening/02-UI-SPEC.md
    resources/js/Components/AppShell.tsx
    app/Http/Controllers/Auth/MagicLinkController.php
    resources/js/bootstrap.js
  </read_first>
  <files>resources/js/Components/AppShell.tsx</files>
  <action>Keep the existing AppShell sign-in dropdown; do not create a new route or auth page. Add a `TurnstileWidget` helper inside `AppShell.tsx` or a small local component in the same file. When `turnstile.enabled` and `turnstile.site_key` are present, load `https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit` once, render the widget into a `mt-4` container within the existing `w-72` form, use compact rendering when available, and set `loginForm.data['cf-turnstile-response']` from the callback token. On expired/error callbacks, clear the token. When disabled, render a hidden input-equivalent data value and no provider widget. Add delivery helper text `The link may take a minute to arrive and expires in 30 minutes.` below the email field or above the widget. Add a checkbox row below Turnstile and above the submit button with visible label `Remember this device`, helper copy `Stay signed in on this device for the configured beta session window.`, and value bound to `remember_device`. Keep submit disabled while `loginForm.processing`; successful submission resets `email`, `cf-turnstile-response`, and `remember_device` to the shared default/bypass state and closes the menu. Keep `aria-expanded`, `aria-controls="sign-in-menu"`, `aria-label="Sign in"`, global flash `role="status"`, and uniform request response behavior unchanged.</action>
  <acceptance_criteria>
    - `rg "challenges.cloudflare.com/turnstile/v0/api.js\\?render=explicit|cf-turnstile-response|Remember this device|Stay signed in on this device for the configured beta session window|The link may take a minute to arrive and expires in 30 minutes" resources/js/Components/AppShell.tsx` finds widget, fields, and required copy.
    - `rg "w-72|p-4|aria-expanded|aria-controls=\\\"sign-in-menu\\\"|role=\\\"status\\\"|aria-live=\\\"polite\\\"" resources/js/Components/AppShell.tsx` finds preserved UI/accessibility contract markers.
    - `npm run typecheck` passes.
    - `npm run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>The embedded sign-in menu submits Turnstile and remember-device fields while preserving the existing compact AppShell interaction contract.</done>
</task>

<task type="auto">
  <name>Task 3: Document auth/session posture and run final gate</name>
  <read_first>
    .planning/phases/02-auth-session-hardening/02-CONTEXT.md
    .planning/phases/02-auth-session-hardening/02-RESEARCH.md
    .planning/phases/02-auth-session-hardening/02-VALIDATION.md
    AGENTS.md
    README.md
    .env.example
    config/session.php
    config/auth.php
    config/services.php
    routes/console.php
    bootstrap/app.php
    package.json
    composer.json
    grumphp.yml
  </read_first>
  <files>README.md</files>
  <action>Per D-09, D-13, and AUTH-05, add a concise README section named `Auth and session beta posture`. Document exact env keys `TURNSTILE_ENABLED`, `TURNSTILE_SITE_KEY`, `TURNSTILE_SECRET_KEY`, `TURNSTILE_BYPASS_TOKEN`, `MAGIC_LINK_EXPIRES_MINUTES`, `MAGIC_LINK_REMEMBER_MINUTES`, `MAGIC_LINK_REMEMBER_DEFAULT`, `SESSION_SECURE_COOKIE`, `SESSION_SAME_SITE`, `SESSION_LIFETIME`, and `SESSION_DOMAIN`. State beta production recommendations exactly: `TURNSTILE_ENABLED=true`, `SESSION_SECURE_COOKIE=true`, `SESSION_SAME_SITE=lax`, host-only `SESSION_DOMAIN=null` unless subdomains are required, and database-backed `SESSION_DRIVER=database`. Document the cleanup command `php artisan auth:prune-magic-links` and that scheduler wiring belongs with production operations. Document that `/entries/upsert` and `/api/sync/push` are session-authenticated web routes protected by Laravel CSRF and same-origin Axios `X-CSRF-TOKEN`. Do not add passkeys, OAuth, passwords, JWT, Sanctum, account deletion, admin tooling, or broad onboarding copy. After documentation, run the final gate commands.</action>
  <acceptance_criteria>
    - `rg "Auth and session beta posture|TURNSTILE_ENABLED=true|SESSION_SECURE_COOKIE=true|SESSION_SAME_SITE=lax|SESSION_DOMAIN=null|auth:prune-magic-links|/entries/upsert|/api/sync/push|X-CSRF-TOKEN" README.md` finds required docs.
    - `rg "passkey|OAuth|password login|JWT|Sanctum|account deletion|admin tooling" README.md` returns no new Phase 2 scope additions.
    - `php artisan test tests/Feature/TurnstileVerifierTest.php tests/Feature/MagicLinkRequestTest.php tests/Feature/MagicLinkConsumeTest.php tests/Feature/MagicLinkCleanupCommandTest.php tests/Feature/SessionWriteProtectionTest.php` passes.
    - `composer test` passes.
    - `npm run typecheck` passes.
    - `npm run build` passes.
    - `XDEBUG_MODE=off vendor/bin/grumphp run` passes.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/TurnstileVerifierTest.php tests/Feature/MagicLinkRequestTest.php tests/Feature/MagicLinkConsumeTest.php tests/Feature/MagicLinkCleanupCommandTest.php tests/Feature/SessionWriteProtectionTest.php && composer test && npm run typecheck && npm run build && XDEBUG_MODE=off vendor/bin/grumphp run</automated>
  </verify>
  <done>Repository docs state the beta auth/session posture and all Phase 2 backend/frontend/final gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inertia shared props -> browser | Public Turnstile config and remember defaults become browser-readable. |
| AppShell form -> magic-link request route | Email, Turnstile token, and remember-device choice cross into server-side auth handling. |
| README operator docs -> production configuration | Documentation directs security-sensitive beta env settings and cleanup operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | `HandleInertiaRequests` | mitigate | Share `site_key`, `enabled`, and local/test `bypass_token` only; never share `TURNSTILE_SECRET_KEY`. |
| T-02-05-02 | Tampering | AppShell Turnstile token | mitigate | Treat widget output as untrusted and rely on Plan 02 server-side `TurnstileVerifier`. |
| T-02-05-03 | Spoofing | remember-device checkbox | mitigate | Submit boolean `remember_device` and rely on Plan 04 persisted token choice during consume. |
| T-02-05-04 | Information Disclosure | request feedback | mitigate | Preserve global uniform request response and avoid Turnstile/rate-limit/account-state details in UI copy. |
| T-02-05-05 | Repudiation | operator posture docs | mitigate | Document exact env keys, cleanup command, session cookie posture, and CSRF write-route protection in README. |
</threat_model>

<verification>
Run:
```bash
npm run typecheck
npm run build
php artisan test tests/Feature/TurnstileVerifierTest.php tests/Feature/MagicLinkRequestTest.php tests/Feature/MagicLinkConsumeTest.php tests/Feature/MagicLinkCleanupCommandTest.php tests/Feature/SessionWriteProtectionTest.php
composer test
XDEBUG_MODE=off vendor/bin/grumphp run
```
</verification>

<success_criteria>
Plan 05 is complete when AppShell submits Turnstile and remember-device fields from the existing dropdown, README documents the beta auth/session posture, and the final Phase 2 quality gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-session-hardening/02-05-SUMMARY.md`.
</output>
